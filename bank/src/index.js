import React from "react";
import ReactDOM from "react-dom/client";
import AllAccountLayout from "./components/AccountLayOut/AllAccountLayOut/AllAccountLayout";
import "./index.css";

const root = ReactDOM.createRoot(document.getElementById("root"));
root.render(
  <React.StrictMode>
    <AllAccountLayout />
  </React.StrictMode>
);
